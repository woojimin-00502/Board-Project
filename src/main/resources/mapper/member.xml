<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thehambiz.test.dao.LoginDao">

	<select id="login_check" resultType="Map">
        SELECT passwd
        FROM dbo.notice_board
        WHERE userid = #{userid}
	</select>

</mapper>